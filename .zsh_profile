if [ -d $HOME/.anyenv ] ; then
    export PATH="$HOME/.anyenv/bin:$PATH"
    eval "$(anyenv init -)"
fi
export PATH="$HOME/.pyenv/shims:$PATH"
export PATH=/Users/nakamurataiki/.pyenv/shims/conda:$PATH
